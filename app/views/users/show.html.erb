<h1 align="center"> Welcome to <%= @user.username %>'s page '</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center gravatar-border">  <!-- me he nventado dos clases para salir del paso, pero con bootstrap 4 -->
 <!-- se quedaría en el centro sin necesidad de esa chapuza-->
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 align="center"><%= @user.username %>'s articles </h4>
  <div class="center">
     <%= will_paginate @user_articles %>  <!-- pongo paginate antes de los articulos y debajo, por eso repite el cpdigo abajo -->
  </div>
  <div class="listing">
    <%= render 'articles/article', obj: @user.articles %>
  </div>
<div class="center">
  <%= will_paginate @user_articles %>
</div>
